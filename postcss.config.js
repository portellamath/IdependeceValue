export default {
  plugins: {
    tailwindcss: {},    // Ativa o plugin do Tailwind CSS para permitir usar utilitários como bg-gray-50, p-4, text-center, etc.
    autoprefixer: {},   // Adiciona automaticamente prefixos para compatibilidade com navegadores diferentes (ex: -webkit-, -moz-)
  },
}
